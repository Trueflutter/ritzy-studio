# ARCHITECT_NOTE

This PR adds the sourced Design Spatial Rules Corpus for the future Design Spatial Intelligence implementation slice.

It is a docs-only research artifact. It does not implement code, change prompts, alter runtime image-generation behavior, change schema/generated types, or authorize production/deploy actions.

Use the corpus as the reviewed source for a later beta-safe implementation slice focused on structured intent capture and designer-facing warnings, especially:

- living-only vs combined living + dining intent
- primary focal point capture
- hard-checkable vs prompt-checkable vs vision-QA-only rule boundaries
- current data gaps that require geometry, wall, window, door, focal-point, and furniture-placement data before deterministic checks are honest

Recommended review posture: strict PR Review Agent review for source quality, scope discipline, checkability classification, and whether the next-slice recommendation remains beta-safe.
